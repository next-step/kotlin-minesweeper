package minesweeper.model.laying

import minesweeper.model.point.Coordinate

class EvenlyStrategy(
    mapHeight: Int,
    mapWidth: Int,
) : MineLayingStrategy {
    override fun layingCoordinates(count: Int): List<Coordinate> {
        TODO("Not yet implemented")
    }
}
